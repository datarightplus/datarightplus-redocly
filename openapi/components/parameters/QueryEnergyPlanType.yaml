name: type
in: query
description: >-
  Used to filter results on the type field.  Any one of the valid
  values for this field can be supplied plus 'ALL'.  If absent defaults to
  'ALL'
explode: true
required: false
schema:
  $ref: ../schemas/EnumEnergyPlanTypeWithAllV1.yaml
style: form
