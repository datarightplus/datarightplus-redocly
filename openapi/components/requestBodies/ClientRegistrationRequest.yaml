description: Client Registration Request
content:
  application/jwt:
    schema:
      type: string
      format: JWT
      description: "The registration request JWT to be used to register with a Provider."
required: true
