type: object
properties:
  baseUri:
    type: string
    format: uri
    example: "https://secure-api.acme.bank/cds-au/v1"
  flags:
    description: The flags supported by this endpoint
    default: [ "SUPPORTS_CDS_VERSIONING" ]
    type: array
    items:
      type: string
      enum:
        $ref: ../flags/CDSDefault.yaml
  requestVersions:
    description: The request versions supported by this endpoint
    default: [ V1 ]
    type: array
    items:
      $ref: ../versions/EnumRequestListScheduledPaymentsSpecificAccounts.yaml
  responseVersions:
    description: The response versions supported by this endpoint
    default: [ V2 ]
    type: array
    items:
      $ref: ../versions/EnumResponseListScheduledPaymentsSpecificAccounts.yaml
required:
  - baseUri
  - flags
  - requestVersions
  - responseVersions
example:
  baseUri: "https://secure-api.acme.bank/cds-au/v1"
  requestVersions: [ V1 ]
  responseVersions: [ V2 ]
  flags: [ "SUPPORTS_CDS_VERSIONING" ]
