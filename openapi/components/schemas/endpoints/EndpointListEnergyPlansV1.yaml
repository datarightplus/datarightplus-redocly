type: object
properties:
  flags:
    description: The flags supported by this endpoint
    default: [ "SUPPORTS_CDS_VERSIONING" ]
    type: array
    items:
      type: string
      enum:
        $ref: ../flags/CDSDefault.yaml
  responseVersions:
    description: The response versions supported by this endpoint
    default: [ V1 ]
    type: array
    items:
      $ref: ../versions/EnumResponseListEnergyPlans.yaml
required:
  - flags
  - responseVersions
example:
  responseVersions: [ V1 ]
  flags: [ "SUPPORTS_CDS_VERSIONING" ]
