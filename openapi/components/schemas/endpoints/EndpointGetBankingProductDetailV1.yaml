type: object
properties:
  baseUri:
    type: string
    format: uri
  flags:
    description: The flags supported by this endpoint
    default: [ "SUPPORTS_CDS_VERSIONING" ]
    type: array
    items:
      type: string
      enum:
        $ref: ../flags/CDSDefault.yaml
  responseVersions:
    description: The response versions supported by this endpoint
    default: [ V4, V5 ]
    type: array
    items:
      $ref: ../versions/EnumResponseGetBankingProductDetail.yaml
required:
  - baseUri
  - flags
  - responseVersions
example:
  baseUri: "https://api.acme.bank/cds-au/v1"
  responseVersions: [ V4, V5 ]
  flags: [ "SUPPORTS_CDS_VERSIONING" ]
