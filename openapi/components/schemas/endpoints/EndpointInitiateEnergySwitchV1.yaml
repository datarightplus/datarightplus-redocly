type: object
properties:
  baseUri:
    description: Base URI for this Operation
    type: string
    format: uri
    example: "https://secure-api.acme.bank/dio-au/v1"
  responseVersions:
    description: The response versions supported by this endpoint
    type: array
    items:
      $ref: ../versions/EnumResponseInitiateEnergySwitchV1.yaml
  requestVersions:
    description: The request versions supported by this endpoint
    type: array
    items:
      $ref: ../versions/EnumRequestInitiateEnergySwitchV1.yaml
required:
  - baseUri
  - requestVersions
  - responseVersions
