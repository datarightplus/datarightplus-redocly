type: object
properties:
  secureBaseUri:
    type: string
    format: uri
    example: "https://secure-api.acme.bank/dio-au/v1"
  endpoint:
    $ref: DiscoverySchemeDioEndpointV1.yaml
required:
  - secureBaseUri
  - endpoint
