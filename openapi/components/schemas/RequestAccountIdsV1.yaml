type: object
properties:
  data:
    type: object
    properties:
      accountIds:
        description: Array of specific account IDs to obtain data for
        items:
          type: string
          x-cds-type: ASCIIString
        type: array
    required:
      - accountIds
  meta:
    type: object
required:
  - data
