description: Type of charge. Assumed to be `OTHER` if absent
default: OTHER
enum:
  - ENVIRONMENTAL
  - REGULATED
  - NETWORK
  - METERING
  - RETAIL_SERVICE
  - RCTI
  - OTHER
type: string
example: OTHER