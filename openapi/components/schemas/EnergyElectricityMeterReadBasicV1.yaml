description: Mandatory if readUType is set to basicRead
properties:
  quality:
    $ref: ./EnumElectricityMeterReadQualityV1.yaml
  value:
    description: Meter read value.  If positive then it means consumption, if
      negative it means export
    type: number
    example: 14.23
required:
  - value
type: object