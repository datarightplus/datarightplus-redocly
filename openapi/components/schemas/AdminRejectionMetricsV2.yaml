description: Number of calls rejected due to traffic thresholds over time
properties:
  authenticated:
    $ref: ./AdminRejectionMetricV1.yaml
  unauthenticated:
    $ref: ./AdminRejectionMetricV1.yaml
required:
  - authenticated
  - unauthenticated
type: object
