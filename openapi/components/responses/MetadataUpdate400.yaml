description: Bad Request
content:
  application/json:
    schema:
      properties:
        errors:
          items:
            $ref: ./MetadataUpdate400Item.yaml
          type: array
      required:
        - errors
      type: object
