description: Unprocessable Entity Error
content:
  application/json:
    schema:
      properties:
        errors:
          items:
            $ref: ./ListProducts422Item.yaml
          type: array
      required:
        - errors
      type: object
