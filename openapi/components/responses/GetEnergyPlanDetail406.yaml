description: Not Acceptable
content:
  application/json:
    schema:
      properties:
        errors:
          items:
            $ref: ./GetEnergyPlanDetail406Item.yaml
          type: array
      required:
        - errors
      type: object
