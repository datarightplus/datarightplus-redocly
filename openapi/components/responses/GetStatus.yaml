description: Success
content:
  application/json:
    schema:
      x-cds-allowMissingDiscriminator: true
      oneOf:
        - $ref: ../schemas/ResponseCommonDiscoveryStatusV1.yaml
headers:
  x-v:
    $ref: ../headers/XV.yaml
