description: Internal Server Error
content:
  application/json:
    schema:
      properties:
        errors:
          items:
            $ref: ../schemas/ErrorGeneralUnexpectedV1.yaml
          type: array
      required:
        - errors
      type: object
