description: Unprocessable Entity Error
content:
  application/json:
    schema:
      properties:
        errors:
          items:
            $ref: ./ListEnergyPlans422Item.yaml
          type: array
      required:
        - errors
      type: object
