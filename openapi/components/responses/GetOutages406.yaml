description: Not Acceptable
content:
  application/json:
    schema:
      properties:
        errors:
          items:
            $ref: ./GetOutages406Item.yaml
          type: array
      required:
        - errors
      type: object
