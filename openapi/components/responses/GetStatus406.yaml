description: Not Acceptable
content:
  application/json:
    schema:
      properties:
        errors:
          items:
            $ref: ./GetStatus406Item.yaml
          type: array
      required:
        - errors
      type: object
