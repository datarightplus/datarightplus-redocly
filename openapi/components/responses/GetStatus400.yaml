description: Bad Request
content:
  application/json:
    schema:
      properties:
        errors:
          items:
            $ref: ./GetStatus400Item.yaml
          type: array
      required:
        - errors
      type: object
