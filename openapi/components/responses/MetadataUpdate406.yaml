description: Not Acceptable
content:
  application/json:
    schema:
      properties:
        errors:
          items:
            $ref: ./MetadataUpdate406Item.yaml
          type: array
      required:
        - errors
      type: object
